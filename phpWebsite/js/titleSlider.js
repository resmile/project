$( document ).ready(function( $ ) {
  $( '#indexSlider' ).sliderPro({
    width: '100%',
    height: 200,
    aspectRatio: 3,
    visibleSize: '100%',
    forceSize: 'fullWidth',
    arrows: true,
    buttons: false
  });
});
